_csv: &csv
  type: spark.SparkDataSet
  file_format: csv
  load_args:
    sep: ','
    header: True
    inferSchema: False

prediction_train:
  type: MemoryDataSet
  # filepath: data/02_intermediate/result.parquet
  #filepath: ${data_path}/${folders.fea}/prediction_train.parquet
  #save_args:
  #  mode: overwrite

prediction_valid:
  type: MemoryDataSet
  # filepath: data/02_intermediate/result.parquet
  #filepath: ${data_path}/${folders.fea}/prediction_valid.parquet
  #save_args:
  #  mode: overwrite