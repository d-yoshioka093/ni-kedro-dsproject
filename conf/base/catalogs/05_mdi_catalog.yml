_csv: &csv
  type: spark.SparkDataSet
  file_format: csv
  load_args:
    sep: ','
    header: True
    inferSchema: False

df_train:
  type: MemoryDataSet
  # filepath: data/02_intermediate/result.parquet
  #filepath: ${data_path}/${folders.fea}/df_train.parquet
  #save_args:
  #  mode: overwrite

df_valid:
  type: MemoryDataSet
  # filepath: data/02_intermediate/result.parquet
  #filepath: ${data_path}/${folders.fea}/df_valid.parquet
  #save_args:
  #  mode: overwrite